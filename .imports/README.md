# .imports/

Auto-extracted contents from .zip files that live inside the repo.

- Source zips remain in-place.
- Each zip extracts to its own folder under `.imports/`.
- See `docs/zip-imports.md` for the extraction report (hashes + destinations).
